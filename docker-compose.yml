services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_BASE_PATH: /uds2_badmin
    container_name: ${CONTAINER_NAME:-podhub}
    restart: unless-stopped
    ports:
      - "${PORT:-3011}:${PORT:-3011}"
    volumes:
      - ./backend/data:/app/data
      - ./backend/backups:/app/backups
      - ./backend/uploads:/app/uploads
    env_file:
      - ./backend/.env
