services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: siga_bot
    restart: unless-stopped
    ports:
      - "3009:3009"
    volumes:
      - ./backend/database.sqlite:/app/database.sqlite
      - ./backend/uploads:/app/uploads
    env_file:
      - ./backend/.env
