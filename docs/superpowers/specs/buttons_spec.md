Investigate and provide sollution for changing buttons visual : 
  -  Welcome post should have :
    first line - one button : catalog
    second line - one button : sale (if it enabled)
    third line - two buttons : 'order' and 'admin'
  - Catalog post should have : 
     all enabled categories' buttons in 2 columns
     next line - sale (if it enabled)
     next line - 2 buttons 'order' and 'admin'
     last line - main_menu
  - product post : 
    first line - 2 buttons 'order' and 'admin'
    second line - back button
    thirt line - main_menu